#include<iostream>   
// using namespace std;           this is to declare name space previosusly this allow us not to use std ever time in main function    
int main(){                        // instead we can directly use cout<<"hello world";       
    std::cout << "hello world " << std:: endl ;  // the name space is std hence std::cout 
    std::cout<<"hii";             // << endl or << std :: endl act as \n or line change           
    return 0;
}